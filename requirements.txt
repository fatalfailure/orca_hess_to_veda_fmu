# Main converter uses Python standard library only.
# GUI requires tkinter, which is bundled with most Python distributions.
# Optional dependencies:
# tkinterdnd2  # in-window drag-and-drop support
# numpy        # Hessian/frequency sanity check
